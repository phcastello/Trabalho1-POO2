version: "3.9"

services:
  db:
    image: postgres:16
    container_name: poo_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: poo
      POSTGRES_USER: poo_user
      POSTGRES_PASSWORD: poo_pass
      # Opcional: define timezone dentro do container
      TZ: America/Sao_Paulo
    ports:
      # 5432 local já está ocupado; mapeamos para 55432
      - "55432:5432"
    volumes:
      # Dados persistentes
      - poo_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB -h 127.0.0.1 -p 5432"]
      interval: 10s
      timeout: 5s
      retries: 5

# Se quiser um client web simples, descomente o Adminer:
#  adminer:
#    image: adminer:4
#    container_name: poo_adminer
#    restart: unless-stopped
#    ports:
#      - "8081:8080"
#    depends_on:
#      db:
#        condition: service_healthy

volumes:
  poo_db_data:
